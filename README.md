A simple, end-to-end URL Shortener application built with Spring Boot 3, Java 21, JPA, and Lombok, with a lightweight HTML + JavaScript UI and RESTful APIs.

🚀 Features

--Shorten long URLs into compact, unique short codes
--Retrieve original URLs using short codes
--RESTful API tested via Postman
--Java 21 compatible
